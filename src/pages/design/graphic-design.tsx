import Head from "next/head";

export default function appDesign() {
  return (
    <div>
      <Head>
        <title>Designo | App Design</title>
        <meta name="description" content=""/>
      </Head>

      im the graphic design page
    </div>
  )
}