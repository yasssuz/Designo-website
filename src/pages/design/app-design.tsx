import Head from "next/head";

export default function appDesign() {
  return (
    <div>
      <Head>
        <title>Designo | Graphic Design</title>
        <meta name="description" content=""/>
      </Head>

      im the app Design page
    </div>
  )
}